# WRECKRUN: AFTERMATH

Играбельный браузерный 3D-прототип аркадной гонки на выживание. Проект сделан на Three.js и Vite, без внешних 3D-моделей: машины, уровни, персонажи и эффекты создаются процедурно.

## Что уже работает

- кампания из десяти районов, включая пять огромных сезонных карт с длинными маршрутами;
- три способа победить: завершить круги, разбить всех соперников или выполнить квоту;
- шесть машин с разными базовыми характеристиками;
- гараж: покупка машин, пять ступеней улучшения двигателя, брони и шасси, покраска;
- таран, пулемёт, ракеты и электрическая катушка;
- AI-соперники с разной агрессивностью и полноценными физическими кузовами;
- текстурированные и анимированные пешеходы с суставным rigid-body ragdoll;
- физика Rapier: масса, импульсы, CCD, четыре ray-cast колеса, подвеска, трение и тормоза;
- локальная деформация вершин кузова в направлении удара и отрывающиеся детали;
- модели машин, колёс, зданий и индустриальных объектов из CC0-паков Kenney;
- дым, искры, огонь, взрывы и частицы крови;
- нитро, миникарта, HUD, результаты заезда и сохранение прогресса в `localStorage`.

## Запуск

```bash
npm install
npm run dev
```

Для локальных сетевых комнат во втором терминале запустите:

```bash
npm run multiplayer
```

Клиент разработки подключается к `ws://localhost:8081`. В production nginx проксирует `/multiplayer` на этот процесс; пример unit-файла находится в `deploy/wreckrun-multiplayer.service`.

Сборка для публикации:

```bash
npm run build
```

Готовая статика появится в `dist/`.

## Управление

- `WASD` или стрелки — газ, тормоз, поворот;
- `Shift` — нитро;
- `Space` — оружие;
- `R` — эвакуация на трассу со штрафом к корпусу;
- `Esc` — пауза.

## Техническая структура

- `src/main.js` — состояние кампании, гараж, генерация 3D-мира, AI и эффекты;
- `src/physics.js` — Rapier, автомобили, подвеска, контакты и ragdoll;
- `src/modelLibrary.js` — загрузка, клонирование и настройка 3D-моделей;
- `src/styles.css` — интерфейс меню, гаража, кампании и HUD;
- `public/assets/ASSET_LICENSES.md` — источники и лицензии 3D-ассетов;
- `index.html` — точка входа Vite.

Это вертикальный срез: он задаёт игровой цикл и визуальное направление. Для production-версии следующим этапом стоит вынести системы в модули, подключить физический движок и заменить процедурные модели оптимизированными GLTF-ассетами.
